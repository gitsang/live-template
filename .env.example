# 本地开发用环境变量覆盖（复制为 .env 后按需修改）
# 该文件本身会被 git 跟踪，.env 不会被跟踪。

# 监听
PORT=8080
HOST=0.0.0.0

# B 站直播间（支持短号，服务端自动解析为真实房间号）
ROOM_ID=90932

# 开发开关：mock 弹幕 + mock 手柄，无需联网/手柄
MOCK=0

# 调试控制条（默认关，OBS 中应保持关闭）
HUD=0

# 视频框透明洞（默认开）
VIDEO_HOLE=1

# 弹幕落盘目录
DATA_DIR=./data

# 日志级别：debug | info | warn | error
LOG_LEVEL=info

# B 站登录态 Cookie（可选）。
# 留空 = 匿名连接：B 站会把**部分**弹幕的昵称打码成「赛***」（实测约 2/3）。
# 填上后打码比例显著下降，但不保证 100% 消除 —— 那是 B 站的行为。
#
# 必须含 DedeUserID 字段；服务启动时会用 nav 接口校验其有效性，
# 校验不通过会降级为匿名并在日志/状态里告警。
#
# ⚠️ 这是账号凭据，等效于登录态，绝不要提交进仓库（.env 已被 gitignore）。
# 生产推荐用 BILI_COOKIE_FILE 指向一个 chmod 600 的文件：
# 环境变量会出现在 docker inspect / 进程 environ / 日志采集里。
# 二者只需其一；都留空即匿名（保持注释掉，否则未登录时会多一条启动告警）。
# BILI_COOKIE=<粘贴浏览器里的完整 Cookie>
# BILI_COOKIE_FILE=./secrets/bili-cookie.txt

# 网页扫码登录的访问口令（可选）。留空则关闭网页登录。
# 开启后：重启 → 日志打印口令 → 打开 /?hud=1 → 点「登录」→ 扫码。
#
# 为什么需要它：二维码登录里持有 qrcode_key 的人就能领走凭据，
# 不设口令则任何人都能抢开挑战、或诱导你扫他的码。这与 HTTPS 无关，
# 因为攻击者走的是你自己的合法接口，流量全程加密也没用。
# LOGIN_TOKEN=随便一串足够长的口令
